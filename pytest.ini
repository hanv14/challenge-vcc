[pytest]
testpaths = tests
addopts = -ra
markers =
    slow: runs real training stages end to end (still seconds, not minutes)
filterwarnings =
    ignore::DeprecationWarning:anndata.*
    ignore::FutureWarning:anndata.*
